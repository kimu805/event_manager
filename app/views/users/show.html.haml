.container.mt-5
  .row
    .col-md-4
      .card
        .card-body.text-center
          - if @user.icon.attached?
            = image_tag @user.image_url, class: "img-fluid rounded-circle border border-secondary shadow h-auto", style: "width: 50px; height: 50px;"
          %h3= @user.name
    .col-md-8
      .card
        .card-body
          %h4 イベント一覧
          - if @events.any?
            %ul.list-group
              - @events.each do |event|
                %li.list-group-item
                  %strong= event.title
                  %p= event.content
                  %small.text-muted= event.created_at.strftime("%Y-%m-%d %H:%M")
          - else
            %p.text-muted イベントはまだありません。
